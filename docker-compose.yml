services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: cpe-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ENVIRONMENT=${ENVIRONMENT:-production}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - LOG_FORMAT=${LOG_FORMAT:-json}
      - PORT=8000
      - SENTRY_DSN=${SENTRY_DSN}
      - ADMIN_USERNAME=${ADMIN_USERNAME}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      # SigNoz / self-hosted OTLP gRPC (optional; omit to disable tracing)
      - OPENTELEMETRY_ENDPOINT=${OPENTELEMETRY_ENDPOINT}
      - OPENTELEMETRY_SERVICE_NAME=${OPENTELEMETRY_SERVICE_NAME}
      - OPENTELEMETRY_INSECURE=${OPENTELEMETRY_INSECURE:-true}
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/career-profile-tool/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NPM_TOKEN: ${NPM_TOKEN}
        REACT_APP_TURNSTILE_SITEKEY: ${REACT_APP_TURNSTILE_SITEKEY}
    container_name: cpe-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - app-network

networks:
  app-network:
    driver: bridge